version: 0.2
env:
  variables:
    DOCKER_BUILDKIT: "1"
phases:
  install:
    commands:
      - ln -sf /usr/share/zoneinfo/Asia/Tokyo /etc/localtime
  pre_build:
    commands: []
  build:
    commands: []
artifacts:
  files: '**/*'
